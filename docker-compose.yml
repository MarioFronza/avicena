services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: AvicenaBD
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: CasaAmarela
    ports:
      - "5432:5432"
    volumes:
      - avicena_postgres_data:/var/lib/postgresql/data

volumes:
  avicena_postgres_data:
